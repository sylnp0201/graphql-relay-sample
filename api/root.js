import { resolve as stories } from 'schema/story';
import { resolve as people } from 'schema/person';

// The root provides a resolver function for each API endpoint
export default {
  stories,
  people,
};
